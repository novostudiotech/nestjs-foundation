# =============================================================================
# Application Configuration
# =============================================================================
# Copy this file to .env or .env.local and fill in your actual values.
# NEVER commit .env or .env.local files to version control!
#
# Security Note: Keep all secrets secure. Use environment-specific values:
#   - Development: Use local/test values
#   - Production: Use secrets management (GitHub Secrets, Vault, etc.)
# =============================================================================

# Application Environment
# Options: development, production, test
# Controls application behavior, logging, and error handling
NODE_ENV=development

# Application Port
# The port on which the application will listen
# Default: 3000
PORT=3000

# Log Level
# Controls the verbosity of application logs
# Options: trace, debug, info, warn, error, fatal
# Default: 'info' in production, 'debug' in development
# Set to 'warn' or 'error' in production to reduce log volume
LOG_LEVEL=debug

# =============================================================================
# Database Configuration
# =============================================================================

# Primary Database URL
# PostgreSQL connection string (required)
# Format: postgresql://user:password@host:port/database?sslmode=require
# Security: Never expose this in logs or client-side code
DATABASE_URL=postgresql://user:password@localhost:5432/dbname?sslmode=require

# Test Database URL (for E2E tests)
# Separate database for running end-to-end tests
# Required when running: pnpm test:e2e
# Should point to a dedicated test database, never production
TEST_DATABASE_URL=postgresql://user:password@localhost:5432/test_dbname?sslmode=require

# =============================================================================
# CORS Configuration
# =============================================================================

# CORS Origin
# Allowed origin for Cross-Origin Resource Sharing
# Set to specific URL (e.g., https://yourdomain.com) or leave empty to allow all
# In production, always specify exact origins for security
CORS_ORIGIN=http://localhost:3000

# =============================================================================
# Authentication Configuration (Better Auth)
# =============================================================================

# Auth Secret
# Secret key for Better Auth encryption and signing (required)
# Must be at least 32 characters long
# Generate a secure random string: openssl rand -base64 32
# Security: Never expose this in logs or client-side code
AUTH_SECRET=your-secret-key-minimum-32-characters-long-change-this-in-production

# Auth Base URL
# Base URL of your application (required)
# Used for generating authentication links and callbacks
# Format: http://localhost:3000 (development) or https://yourdomain.com (production)
AUTH_BASE_URL=http://localhost:3000

# Auth Base Path (optional)
# Base path for Better Auth API endpoints
# Default: /api/auth
# Change only if you need to customize the auth endpoint path
# AUTH_BASE_PATH=/api/auth
